# Security Logger Frontend Configuration

# Flask Backend URL (default: http://localhost:5000)
NEXT_PUBLIC_BACKEND_URL=http://localhost:5000

# Force mock data mode (default: false)
# Set to 'true' to use Next.js mock API routes instead of Flask backend
NEXT_PUBLIC_USE_MOCK_DATA=false
